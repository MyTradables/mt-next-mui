import { makeStyles } from '@mui/styles';

const layoutStyles = makeStyles({
    main: {
        minHeight: '80vh'
    },
    footer: {
        textAlign: 'center'
    }
});

export default layoutStyles;